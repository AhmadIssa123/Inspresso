# Add Raspberry Pi hardware libraries here after the final sensors and
# interfaces are selected. Keeping them separate allows development and
# testing on Windows/macOS without Raspberry Pi-specific dependencies.
